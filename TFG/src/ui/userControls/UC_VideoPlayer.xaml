<UserControl x:Class="TFG.src.ui.userControls.UC_VideoPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <MediaElement AllowDrop="True" 
            Name="myMediaElement" 
            Margin="11,0,9,106" 
            RenderTransformOrigin="0.5,0.5" 
            LoadedBehavior="Manual">
        </MediaElement>
        <StackPanel Orientation="Horizontal" Margin="0,0,10,10" VerticalAlignment="Bottom">

            <!-- Play button. -->
            <Button  Click="OnMouseDownPlayMedia" Name="Play" FontSize="15" >[|>]</Button>
            <!-- Pause button. -->
            <Button Click="OnMouseDownPauseMedia" Name="Pause" FontSize="15">[II]</Button>
            <!-- Stop button. -->
            <Button Click="OnMouseDownStopMedia" Name="Stop" FontSize="15">[#]</Button>
            <Slider Minimum="0" Maximum="1" Width="100" 
                                                Value="{Binding Path=Volume,ElementName=myMediaElement}" />
            <CheckBox Name="SecondPerSecond" Click="SecondPerSecond_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
